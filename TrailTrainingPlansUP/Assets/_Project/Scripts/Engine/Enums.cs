namespace Romero.TrailTrainingPlans.Data
{
    public enum weekTrainingType
    {
        Rest,
        Intensity,
        Base,
        Specific,
        Goal
    }

    public enum sportType
    {
        TrailRunning,
        Running,
    }
}